<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lawnscape">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ProfileSettingsActivity"
            android:label="@string/title_activity_profile_settings" />
        <activity
            android:name=".PostJobActivity"
            android:label="@string/title_activity_post_job" />
        <activity
            android:name=".ViewMyPostsActivity"
            android:label="@string/title_activity_view_my_posts" />
        <activity
            android:name=".ViewAllJobsActivity"
            android:label="@string/title_activity_view_all_jobs" />
        <activity android:name=".ViewSingleJobActivity" />
        <activity android:name=".ViewMySavedPostsActivity"></activity>
    </application>

</manifest>